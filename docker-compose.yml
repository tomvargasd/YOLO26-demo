version: '3.8'

services:
  yolo-demo:
    build: .
    container_name: yolo26-demo
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
      - ENVIRONMENT=production
    restart: unless-stopped
