version: '3.8'

services:
  yolo-demo:
    build: .
    container_name: yolo26-demo
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models
      - ./cert.pem:/app/cert.pem:ro
      - ./key.pem:/app/key.pem:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
